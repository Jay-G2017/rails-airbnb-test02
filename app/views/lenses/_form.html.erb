<%= form_with(model: lense, local: true) do |form| %>
  <% if lense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lense.errors.count, "error") %> prohibited this lense from being saved:</h2>

      <ul>
      <% lense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :lense_name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :lense_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
